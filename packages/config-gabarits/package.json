{
  "name": "@sama-emi/config-gabarits",
  "version": "0.1.0",
  "private": true,
  "description": "Gabarits de documents (scénario pédagogique, parcours de formation) et blocs fixes, stockés en JSON versionné plutôt qu'en dur dans le code.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"require('fs').cpSync('src/gabarits','dist/gabarits',{recursive:true})\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.3"
  }
}
